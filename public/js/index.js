const form=document.getElementById('loginForm'),error=document.getElementById('error');
form.addEventListener('submit',async e=>{e.preventDefault();error.classList.add('hidden');const studentId=document.getElementById('studentId').value.trim().toUpperCase();try{const data=await App.api('/validate',{method:'POST',body:JSON.stringify({studentId})});sessionStorage.setItem('student_id',data.student_id);location.href='/home.html';}catch(err){error.textContent=err.message;error.classList.remove('hidden')}});
